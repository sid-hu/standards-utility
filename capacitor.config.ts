import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personal.standardsutility',
  appName: 'standards-utility',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
